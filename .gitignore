node_modules/
.env
.expo/
dist/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
android/app/build/
ios/build/

# Capturas de pantalla pegadas en el chat durante pruebas manuales — no
# son assets del proyecto (esos viven en assets/screenshots/).
/Imagenes App apk pruebas/

# Archivos de trabajo temporales (ej. artefactos publicados), no parte del proyecto
/scratch/

# Builds descargados (.aab/.apk) para subir a Play Console a mano — son
# binarios de 60+ MB, no pertenecen al repo (se generan con EAS/GitHub Actions)
*.aab
*.apk
verity-production-aab.zip
